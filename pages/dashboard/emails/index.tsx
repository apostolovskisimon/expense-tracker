import React from "react";
import Layout from "../../../components/Layout";

type Props = {};

const Emails = (props: Props) => {
  return (
    <Layout>
      <div>this is Emails</div>
    </Layout>
  );
};

export default Emails;
